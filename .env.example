# Server (Express)
PORT=8787
# Your OpenAI API key (server-side; do not expose to clients)
OPENAI_API_KEY=sk-...
# Rate limit: max requests per IP per 24h
DAILY_LIMIT=50
# Rate limit: max requests per IP per minute
MINUTE_LIMIT=5
# Optional: Redis URL for distributed rate limiting (leave empty to use memory)
REDIS_URL=